---
description: Data and migration standards for PostgreSQL default and MySQL optional profiles, including schema, Flyway/Liquibase, and query performance.
globs: "backend/**/*.{java,sql,yml,yaml}"
alwaysApply: false
---

# Backend Data Standards (PostgreSQL + MySQL)

## Database Strategy
- PostgreSQL is default for local/dev and standard deployments.
- MySQL is optional for compatibility/migration environments.
- Keep DB-specific settings behind Spring profiles (`postgres`, `mysql`).

## Configuration Templates
- Keep shared config in `application.yml`.
- Keep vendor config in `application-postgres.yml` and `application-mysql.yml`.
- Keep secrets in environment variables, never hardcoded.

## Migration Rules
- Use one migration tool consistently (Flyway or Liquibase).
- Keep migration folders split:
  - `db/migration-postgres` (default)
  - `db/migration` (MySQL compatibility)
- Prefer additive, backward-compatible migrations.

## Schema and Query Rules
- Use snake_case for table and column names.
- Keep audit fields: `created_at`, `updated_at`, optional `deleted_at`.
- Use UUID external IDs consistently; keep PK/FK types aligned.
- Keep query loading strategy aligned with `spring-transaction-rules.mdc`.

## Indexing and Capacity
- Add indexes for common filter/sort/join paths.
- Keep high-traffic query tuning aligned with `spring-db-performance.mdc`.
