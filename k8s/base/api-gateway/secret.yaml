apiVersion: v1
kind: Secret
metadata:
  name: api-gateway-secret
  labels:
    app: api-gateway
    component: backend
type: Opaque
stringData:
  SENTRY_DSN: ""  # Optional, will be overridden by overlays