apiVersion: v1
kind: Secret
metadata:
  name: crm-service-secret
  labels:
    app: crm-service
    component: backend
type: Opaque
stringData:
  DB_PASSWORD: "crm_password"  # Will be overridden by overlays
  SENTRY_DSN: ""  # Optional, will be overridden by overlays