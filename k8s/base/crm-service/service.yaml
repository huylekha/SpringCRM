apiVersion: v1
kind: Service
metadata:
  name: crm-service
  labels:
    app: crm-service
    component: backend
    tier: service
spec:
  type: ClusterIP
  ports:
  - port: 8082
    targetPort: 8082
    protocol: TCP
    name: http
  selector:
    app: crm-service