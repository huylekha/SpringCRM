apiVersion: v1
kind: ConfigMap
metadata:
  name: frontend-config
  labels:
    app: frontend
    component: frontend
data:
  NEXT_PUBLIC_API_BASE_URL: "http://localhost/dev/api-gateway/api/v1"  # Will be overridden by overlays
  NEXTAUTH_URL: "http://localhost/dev/frontend"  # Will be overridden by overlays